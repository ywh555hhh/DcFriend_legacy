# .env.example
# 复制为 .env 并填入你自己的值

# .env (最终权威版本)

# 这个变量名必须是 DISCORD_BOT_TOKEN，因为 config.py 的 alias 需要它
DISCORD_BOT_TOKEN="YOUR_DISCORD_BOT_TOKEN_HERE"

# 这个变量名必须是 GOOGLE_AI_KEY，因为 config.py 的 alias 需要它
GOOGLE_AI_KEY="fakeit"

# Google AI Model Name (optional, has a default)
GOOGLE_AI_MODEL_NAME="models/gemini-2.5-flash-preview-05-20"

